#include <iostream>

using namespace std;

// Definizione delle funzioni

int main() {
    int cassette[] = {10, 20, 30, 40, 50};
    int n = sizeof(cassette) / sizeof(cassette[0]);

    cout << "L'inventario totale delle merci è: " << /*...*/ << endl;
    
    return 0;
}

// Implemenetazione delle funzioni
