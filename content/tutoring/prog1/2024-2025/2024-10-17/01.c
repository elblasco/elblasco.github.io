#include <stdio.h>

// Definizione delle funzioni

int main() {
    int cassette[] = {10, 20, 30, 40, 50};  // Un esempio di array con le quantità nelle cassette
    int n = sizeof(cassette) / sizeof(cassette[0]);

    printf("L'inventario totale delle merci è: %d\n", /*....*/);
    
    return 0;
}

// Implemenetazione delle funzioni
