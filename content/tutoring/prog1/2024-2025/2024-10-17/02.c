#include <stdio.h>

// Definzione delle funzioni

int main() {
    int cassette[] = {8, 6, 5, 9};
    int guadagni[] = {10, 15, 12, 8};
    int n = sizeof(cassette) / sizeof(cassette[0]);
    int x = 10;
    
    printf("Il guadagno totale della giornata è: %d\n", /*...*/);
    
    return 0;
}

// Implemenetazione delle funzioni

